# Local secrets and environment overrides
.env
.env.local
.env.*.local

# Public production build defaults are committed so Vercel builds live mode.
!.env.production

# OS and editor noise
.DS_Store

# Local isolated feature worktrees
.worktrees/
